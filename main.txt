import yfinance as yf
from engine.analytics import daily_returns
from strategies.signals import sma_trend_signal, momentum_signal

if __name__ == "__main__":
    prices = yf.download("SPY", start="2018-01-01", auto_adjust=True)["Close"]

    trend = sma_trend_signal(prices)
    momentum = momentum_signal(prices)

    print("Trend signal (last 5):")
    print(trend.tail())

    print("\nMomentum signal (last 5):")
    print(momentum.tail())